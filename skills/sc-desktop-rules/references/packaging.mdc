---
description: 桌面端打包流程、electron-builder、正式环境产物清理（注释 / console）与 tree-shaking
globs: "**/{electron.vite.config.ts,electron-builder.yml,scripts/build.mjs,package.json}"
alwaysApply: false
---

# 打包与发布（SCRM Chat Desktop）

## 技术栈与产物

| 环节 | 工具 | 说明 |
| --- | --- | --- |
| 源码构建 | [electron-vite](https://electron-vite.org/) | 拆分 **main / preload / renderer** 三套 Vite 配置，入口见仓库根目录 `electron.vite.config.ts`。 |
| 安装包 | [electron-builder](https://www.electron.build/) | 配置见 `electron-builder.yml`；安装包输出在 `dist/`。 |
| Vite 编译产物 | Vite `outDir`（electron-vite 默认） | 主进程与预加载等输出在 `out/`（与 `package.json` 的 `"main": "./out/main/index.js"` 一致）。 |

## npm/pnpm 脚本

| 脚本 | 作用 |
| --- | --- |
| `pnpm dev` | 开发：`electron-vite dev`，热更新；**不**应用下文「正式环境清理」。 |
| `pnpm build:vite` | 仅执行 `electron-vite build`（默认 `mode=production`，见 Vite CLI）。 |
| `pnpm build:electron` | 交互式 `scripts/build.mjs`：输入版本、选择 **development / production / both**，依次执行图标生成、`electron-vite build --mode <env>`、`electron-builder -c electron-builder.yml`。 |
| `pnpm icons:generate` / `predev` | 生成/更新图标资源，打包前由 `build.mjs` 也会调用。 |

## electron-builder 要点

- **打包范围**：`electron-builder.yml` 的 `files` 排除源码与脚本，将编译结果与依赖打入应用；`extraResources` 将 `resources/icons` 拷到安装包内 `icons`。
- **原生模块**：`asarUnpack` 含 `**/*.node`（如 `better-sqlite3`）以便运行时加载。
- **多环境**：`scripts/build.mjs` 通过 `--mode development|production` 区分构建模式；**正式环境清理**仅当 `mode === 'production'` 时启用（见下节）。

## 正式环境：清除注释与 `console`（已实现）

**目标**：发布包内业务代码中不应再出现手写 `console.log` / `console.warn` 等调用，并减少产物中的注释（压缩与 `legalComments` 策略）。

**实现位置**：`electron.vite.config.ts` 中根据 `mode` 注入 `prodPack`：

1. **`build.minify: 'esbuild'`**  
   使用 esbuild 压缩，配合下列选项去掉输出中的注释（含常规块注释/行注释及 LICENSE 类注释策略）。

2. **`esbuild.drop: ['console', 'debugger']`**  
   在 **main / preload / renderer** 三套配置中均合并生效，移除对 `console` 命名空间与 `debugger` 语句的调用（含 `console.log` 等）。  
   - 不影响 **electron-log** 等通过独立模块写入的日志（非 `console.*`）。  
   - 对「字符串里写死的 `'console.log'`」等无效果（仅处理真实 AST 调用）。

3. **`esbuild.legalComments: 'none'`**  
   打包输出中不保留 legal 注释块，减少体积与信息泄露；与 minify 共同作用，**正式包中不应依赖从注释读取的运行时信息**。

**未启用时机**：`mode === 'development'`（例如 `electron-vite build --mode development`）**不**合并 `prodPack`，保留注释与 `console`，便于联调与排查。

**自检建议**：`pnpm exec electron-vite build --mode production` 后，在 `out/` 与 `out/renderer` 等产物中搜索 `console.log`，应无业务侧直接调用（第三方依赖 bundle 内仍可能含其自身逻辑，属正常）。

## 正式环境：Tree-shaking（摇树，已实现）

**目标**：在 **production** 打包中尽可能移除未引用的 ESM 导出与无副作用模块，减小 `out/` 与安装包体积；与「仅压缩」不同，摇树依赖 **静态 `import`/`export` 图** 与 **副作用（side effects）** 标注。

**实现要点**：

1. **根目录 `package.json` 的 `sideEffects` 字段（必读）**  
   - 标明「仅这些路径在导入时有副作用」（如全局样式、Vue SFC、入口 `main.ts`）。  
   - 供 Rolldown/Vite 判断哪些模块可安全剔除；**勿**写成 `false` 全量无副作用，否则会误删仅用于样式的 `import './foo.scss'`。  
   - 当前约定：`*.css` / `*.scss` / `*.sass` / `*.vue` 与 `src/renderer/src/main.ts`；若新增仅副作用入口（如独立 `polyfill.ts`），需追加到数组。

2. **`electron.vite.config.ts` 中 `build.rolldownOptions.treeshake`（production）**  
   - Vite 8 默认使用 **Rolldown**；打包选项写在 `build.rolldownOptions`（`rollupOptions` 为兼容别名，会合并到同一套配置）。  
   - 配置 `treeshake.moduleSideEffects: 'no-external'`：对 **external** 模块采用「无额外副作用」假设，更易剔除未使用的外部依赖片段；**本地/工作区源码**仍按默认与 `sideEffects` 字段处理。  
   - 若某第三方包被误删，可改为规则数组或为该包补充 `sideEffects` 声明（优先查包作者是否已正确标注）。

3. **与「清除注释」的关系**  
   - 摇树在 **模块图与 AST** 层删未使用代码；**minify + `legalComments`** 在输出层删注释。二者互补，不是替代关系。

**未启用时机**：`mode !== 'production'` 时不合并 `prodPack` 中的 `rolldownOptions` 与上述 esbuild 清理，便于开发构建排查问题。

## 修改打包行为时的注意点

- 调整「是否剔除 console」时，应同时检查 **主进程 / preload / 渲染进程** 三套配置是否一致（当前为共用同一份 `prodPack` 展开）。
- 调整 tree-shaking 时，需同步检查 **`package.json#sideEffects`** 与 **`rolldownOptions.treeshake`**，避免样式或副作用入口被误摇掉。
- 若升级 Vite 后 `esbuild` / `rolldownOptions` 顶层选项有变更，以官方 `UserConfig` 与 [Rolldown 配置](https://rolldown.rs/) 为准，并同步更新本节与 `electron.vite.config.ts` 注释。
