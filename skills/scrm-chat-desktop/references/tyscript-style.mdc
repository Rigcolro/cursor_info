---
description: TypeScript 代码风格规范
alwaysApply: true
---

# TypeScript 代码风格规范

## 数据处理
- 接口响应数据各个字段在赋值前，应该先判定是否为对应类型，避免类型错误。类型正确时，再进行赋值操作；类型错误时，作兼容处理
- 组件中/非组件中使用$to解构异步请求结果，如：`const [err, resp] = await $to(loginApi.login(form.value))`


